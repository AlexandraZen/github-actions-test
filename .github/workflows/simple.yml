name: Commandes Shell

on: [push]
jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run : echo "Hello Kitten"
      - name: multiline script
        run: |
          node -v
          npm -v
